name: blog deploy

on:
  push:
    branches:
      - master

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'

#      - run: npm ci
      - run: npm install
      - run: npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3.2.0
        env:
          deploy_key: ${{ secrets.PERSONAL_TOKEN }}
          personal_token: ${{ secrets.PERSONAL_TOKEN }}
          external_repository: woytu/woytu.github.io.git
          PUBLISH_BRANCH: master
          publish_dir: ./docs
          force_orphan: true
          user_name: woytu
          user_email: admin@woytu.com